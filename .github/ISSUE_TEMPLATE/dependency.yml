name: "🔗 Dependency"
description: Register a dependency for traceability and impact analysis.
title: "🔗 [Dependency] <from> → <to>"
labels: ["type:dependency","status:registered"]
body:
  - type: input
    id: fromElement
    attributes:
      label: fromElement
      description: upstream element (repo / service / initiative / epic)
    validations: { required: true }
  - type: input
    id: toElement
    attributes:
      label: toElement
      description: downstream element impacted
    validations: { required: true }
  - type: textarea
    id: rationale
    attributes:
      label: rationale
      description: why this link exists & usage pattern
  - type: checkboxes
    id: automationOptions
    attributes:
      label: automationOptions
      options:
        - label: updateDependencyGraph
        - label: triggerRippleImpactChecks
